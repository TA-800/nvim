require("options")
require("mappings")
require("autocmds")
require("plugin")
--require("autoload") -- TODO: Let's use this as post plugin installation autocmds or mappings to load (require('plugin').setup {}) on demand?
